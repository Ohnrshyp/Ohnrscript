"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
// ohn-zod.ohn
// Official Ohnrscript Standard Library Package
// Zero-allocation Ahead-Of-Time validation decorators.

/**
 * Marks a class for CBOR Ahead-of-Time serialization.
 * Stripped by Ohnrscript compiler at build time.
 */
function cbor(target) {
  // AOT Compiler removes this
}

/**
 * Validates a 32-bit unsigned integer property.
 * Stripped by Ohnrscript compiler at build time.
 */
function uint32(rules) {
  return function (target, propertyKey) {
    // AOT Compiler removes this
  };
}

/**
 * Validates a string property.
 * Stripped by Ohnrscript compiler at build time.
 */
function string(rules) {
  return function (target, propertyKey) {
    // AOT Compiler removes this
  };
}
module.exports = {
  cbor: cbor,
  uint32: uint32,
  string: string
};